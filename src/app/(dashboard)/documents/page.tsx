import { DocumentsModule } from "@/components/modules/documents";

export const metadata = { title: "Dokumente — MW Transport Service" };

export default function DocumentsPage() {
  return <DocumentsModule />;
}
