import { ShiftPlanning } from "@/components/modules/shiftplanner";

export const metadata = { title: "Schichtplanung — MW Transport Service" };

export default function ShiftPlannerPage() {
  return <ShiftPlanning />;
}
