import { CalendarView } from "@/components/modules/misc";

export const metadata = { title: "Kalender — MW Transport Service" };

export default function CalendarPage() {
  return <CalendarView />;
}
