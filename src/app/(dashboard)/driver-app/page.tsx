import { DriverApp } from "@/components/modules/driverapp";

export const metadata = { title: "Fahrer-App — MW Transport Service" };

export default function DriverAppPage() {
  return <DriverApp />;
}
