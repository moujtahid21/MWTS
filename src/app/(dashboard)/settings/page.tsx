import { Settings } from "@/components/modules/settings";

export const metadata = { title: "Einstellungen — MW Transport Service" };

export default function SettingsPage() {
  return <Settings />;
}
