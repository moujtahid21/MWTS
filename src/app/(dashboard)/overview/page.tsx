import { Dashboard } from "@/components/modules/dashboard";

export const metadata = { title: "Übersicht — MW Transport Service" };

export default function OverviewPage() {
  return <Dashboard />;
}
